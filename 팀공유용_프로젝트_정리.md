# HedgeMate 팀 공유용 프로젝트 정리

작성일: 2026-05-27  
프로젝트명: HedgeMate  
주제: AI 기반 포트폴리오 리스크 진단 및 헤지 추천 서비스

## 1. 한 줄 소개

HedgeMate는 사용자가 보유 자산을 입력하면 시장 장세, 포트폴리오 취약도, 스트레스 테스트 결과를 함께 분석하고, 실제 추천 기준을 통과한 헤지 후보를 제시하는 금융 인공지능 실습 프로젝트다.

## 2. 핵심 목표

- 보유 포트폴리오의 시장 리스크를 정량적으로 진단한다.
- 장세 시나리오와 이벤트 오버레이를 활용해 현재 시장 상태를 요약한다.
- 헤지 후보를 단순 점수순으로 보여주지 않고, Gate 기준을 통과한 후보와 참고안을 구분한다.
- 추천 근거, 실패 사유, 데이터 신뢰도, 백테스트 결과를 같이 보여줘 사용자가 판단할 수 있게 한다.

## 3. 프로젝트 구성

| 경로 | 역할 |
| --- | --- |
| `hedge-front/` | React + Vite 기반 프론트엔드 앱 |
| `HedgeMate/` | Python 기반 분석 엔진, 대시보드 API, 산출물 관리 |
| `scenario_research/` | 시장 시나리오 리서치, 장세 판단, 이벤트 오버레이 실험 공간 |
| `docs/` | 실행 결과 및 보고서성 문서 |
| `output/`, `outputs/` | 테스트, 브라우저 QA, 임시 산출물 |
| `HedgeMate/outputs/` | 실제 HedgeMate 분석 산출물과 manifest |

## 4. 주요 화면

프론트엔드 라우팅 기준 주요 화면은 다음과 같다.

- `/` 온보딩
- `/register` 포트폴리오 등록
- `/portfolios` 내 포트폴리오
- `/report` 개선 리포트
- `/analysis` 자산 분석
- `/sensitivity` 자산 민감도
- `/market-state` 시장 상태 대시보드
- `/strategy` 전략
- `/stress-test` 스트레스 테스트
- `/settings` 설정

## 5. 실행 방법

### 5.1 백엔드/API 서버

프론트엔드 개발 서버는 기본적으로 `/api` 요청을 `http://127.0.0.1:8766`으로 프록시한다. 따라서 팀원이 화면까지 확인하려면 백엔드를 먼저 8766 포트로 실행하는 것이 좋다.

```powershell
cd HedgeMate
python scripts/serve_dashboard.py --port 8766
```

서버 단독 확인 주소:

```text
http://127.0.0.1:8766
```

### 5.2 프론트엔드

```powershell
cd hedge-front
npm install
npm run dev
```

브라우저 확인 주소:

```text
http://localhost:5173
```

다른 백엔드 포트를 쓰는 경우:

```powershell
$env:VITE_HEDGEMATE_API_URL="http://127.0.0.1:8080"
npm run dev
```

## 6. 검증 방법

Python 테스트:

```powershell
pytest
```

프론트엔드 빌드:

```powershell
cd hedge-front
npm run build
```

프론트엔드 린트:

```powershell
cd hedge-front
npm run lint
```

현재 `pytest.ini` 기준 테스트 대상은 `HedgeMate/tests`와 `scenario_research/tests`다.

## 7. 추천 판정 기준

HedgeMate 결과는 아래 세 가지로 나눈다.

| 분류 | 의미 |
| --- | --- |
| 추천안 | 계산 가능하고 모든 Gate 기준을 통과한 후보 |
| 참고안 | 계산은 가능하지만 Gate 기준을 통과하지 못한 후보 |
| 실패/제외 | 비교 가능한 결과 자체를 만들기 어려운 입력 또는 자산 |

추천안 Gate 기준:

- CVaR 개선 > 0
- MDD 개선 >= 0
- Stress 평균수익률 개선 >= 0
- 상관 또는 Beta 감소
- 유동성 기준 통과
- DQ FAIL 자산 제외

운영 원칙:

- 추천안이 있으면 추천안을 우선 노출한다.
- 추천안이 0건일 때만 참고안을 전면 노출한다.
- 참고안은 추천안의 대체물이 아니라, 계산 가능한 참고 결과다.

## 8. 시나리오 리서치 구조

`scenario_research/`는 HedgeMate 본체와 분리된 실험 공간이다.

- Phase 4: 정형 시장 proxy 기반 장세 판단
- Phase 5: 뉴스/정책 이벤트 오버레이
- Phase 6: 정형 점수와 이벤트 오버레이의 최종 병합
- Intraday nowcast: 1시간봉 기반 한국장, 원화, 반도체 장중 반응 감지

핵심 산출물 예시:

- `outputs/processed/scenario_state_daily_<run_id>.csv`
- `outputs/final/final_market_state_daily_<run_id>.csv`
- `outputs/final/scenario_confidence_<run_id>.csv`
- `outputs/reports/final_market_state_summary_<run_id>.md`
- `outputs/nowcast_vectors/current_intraday_nowcast_1h_<run_id>.csv`

## 9. 팀 역할 분담안

| 역할 | 담당 범위 |
| --- | --- |
| 프론트엔드 | 화면 흐름, 포트폴리오 입력 UX, 리포트/대시보드 시각화 |
| 백엔드/API | `serve_dashboard.py`, API 응답 구조, manifest 및 산출물 연결 |
| 분석 엔진 | 헤지 후보 생성, Gate 기준, 백테스트, 추천/참고안 분류 |
| 시나리오 리서치 | 장세 판단, 이벤트 오버레이, nowcast 실험 및 검증 |
| QA/문서 | 테스트 실행, 발표 자료, 결과 캡처, 팀 공유 문서 정리 |

## 10. 발표 때 강조할 포인트

- 단순 추천 시스템이 아니라 추천 가능성 검증 기준을 분리했다.
- 추천안과 참고안을 명확히 구분해 과신을 줄였다.
- 장세 시나리오와 이벤트 오버레이를 함께 사용하지만, 이벤트는 보조 신호로 제한했다.
- 프론트엔드에서 포트폴리오 입력부터 리포트 확인까지 하나의 사용자 흐름으로 연결했다.
- 테스트와 산출물 manifest를 통해 현재 분석 결과와 입력 포트폴리오가 일치하는지 확인한다.

## 11. 공유 전 체크리스트

- 백엔드 `python scripts/serve_dashboard.py --port 8766` 실행 가능
- 프론트엔드 `npm run dev` 실행 가능
- `http://localhost:5173`에서 주요 화면 접근 가능
- `pytest` 통과 여부 확인
- `npm run build` 통과 여부 확인
- 발표용 캡처는 `output/playwright/` 또는 브라우저 캡처로 별도 저장

## 12. 팀원이 먼저 봐야 할 파일

- `팀공유용_프로젝트_정리.md`
- `HedgeMate/추천판정기준.md`
- `scenario_research/README.md`
- `hedge-front/README.md`
- `hedge-front/src/App.jsx`
- `HedgeMate/scripts/serve_dashboard.py`
